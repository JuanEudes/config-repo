server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cibertec
    username: Yagami
    password: juan2024
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: course-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/  
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  security:
    enabled: false
